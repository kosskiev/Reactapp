# Все проделанные шаги
# подключение к удаленному серверу если доступ к серверу предоставляется другой стороной нужно получить IP сервера, имя пользователя, ключи публичный и приватный
# после этого прописываем эти ключи в данные файлы id_rsa и id_rsa.pub по адресу cd ~/.ssh 
# подключаемся root - имя пользователя, ip - сервера
ssh root@ip

# после подключения устанавливаем на сервере нужные программы и их версии
# версия Node.js v14.x:
curl -fsSL https://deb.nodesource.com/setup_14.x | sudo -E bash -
sudo apt-get install -y nodejs
# Вместе с ним установился npm version 6.14.10

# ставлю yarn version 1.22.10 (на момент установки последняя версия)
npm install -g yarn
yarn -v

# Создаем папку приложения и копируем в него репозиторий с git
mkdir app
cd app
git init
git clone git@github.com:kosskiev/Reactapp.git

# устанавливаем npm (установка всех зависимостей, перечисленных в package.json)
npm install
# Запускает приложение в режиме разработки. Откройте http: // localhost: 3000, чтобы просмотреть его в браузер
yarn start
# Запускает тестовый раннер в интерактивном режиме часов.
yarn test
# yarn test -- --watchAll=false     Для запуска тестов в неинтерактивном режиме используйте следующую команду:

# Создает приложение для производства в buildпапку. Он правильно связывает React в производственном режиме и оптимизирует сборку для достижения максимальной производительности.
yarn build
